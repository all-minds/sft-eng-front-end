import "@testing-library/jest-dom";
import PropertyCard from "@/components/property-card";

describe("Property card component", () => {
  it("Should has card component", () => {});

  it("Should has image brand on card", () => {});
});
